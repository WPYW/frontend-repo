import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;

  color: var(--scroll-catcher-text-color);

  height: 100px;
`;

export const NoMoreProject = styled.div``;
