import styled from 'styled-components';

export const Wrapper = styled.input`
  all: unset;

  font-size: var(--base-text-size-medium);
  color: var(--modal-text-input-text-color);

  padding-left: 4px;
  padding-top: 4px;
`;
